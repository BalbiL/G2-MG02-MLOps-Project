OLTP Database (Supabase)

# User
user (user_id)
user_topics (user_id, topics_list)

# News and interactions
news (news_id, category, subcategory, title, abstract)
interactions_static (same columns as interactions table) (a more cleaned version of behaviors data of MIND dataset / should only be used for training purposes)
interactions (user_id, news_id, event_type, event_time) (the future interactions will be stored here)

# Recommendations
default_recs (news_id, score, generation_time) => recs based on the date, general for everybody
personalized_recs (user_id, news_id, score, generation_time) => (store dynamically recs about a defined user)


Vector DB (Pgvector, Milvus, Chroma)
news_vec (news_id, vec)

